import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.aistudioapplet',
  appName: 'AI Studio Applet',
  webDir: 'dist'
};

export default config;
